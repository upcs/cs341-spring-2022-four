/* hike.page.template
 * Brynn Harrington 
 * Date Modified: 20 Mar 2022
 *
 * This unit test verifies a user's account 
 * is successfully created in the server
 * 
 * STATUS: ?
 *
 */
/* GLOBAL VARIABLES */
var fs = require('fs'); //require the file system
var $ = require('jquery'); //require the file system
// window SOURCE: https://www.npmjs.com/package/window
// allows testing for the HTML
window.$ = $;